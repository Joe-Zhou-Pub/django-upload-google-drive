runtime: python
env: flex
entrypoint: gunicorn -b :$PORT mysite.wsgi

runtime_config:
  python_version: 3

env_variables:
  DB_PASS: 

beta_settings:
  cloud_sql_instances: xrp2-files-app1:us-central1:xrp2sql2
 
